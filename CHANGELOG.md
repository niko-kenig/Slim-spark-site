## [2026-04-14] - Лендинг Slim-Spark под PWA
**What was done:**
- Создан новый лендинг на React + TypeScript с Tailwind CSS по структуре из 6 блоков.
- Полностью обновлён контент главного экрана, программы, этапов, блока результатов, CTA и футера.
- Удалены кнопки App Store/Google Play и добавлены целевые кнопки входа в PWA на `app.slim-spark.ru`.
- Заменена секция отзывов на нейтральный блок для пилотного запуска без вымышленных данных.
- Добавлен блок консультации в Telegram и оставлены только VK/Telegram в футере.
- Обновлена сборка под совместимую версию Vite для текущей версии Node.js.

**Files modified:**
- `src/App.tsx` — собрана новая структура лендинга и все тексты/кнопки по ТЗ.
- `src/main.tsx` — переключён импорт на named export компонента.
- `src/index.css` — подключён Tailwind и добавлены базовые глобальные стили.
- `vite.config.ts` — подключён плагин Tailwind для Vite.
- `package.json` / `package-lock.json` — добавлены зависимости Tailwind и обновлены версии Vite-плагинов для совместимости.

**Notes:**
- Выбран стек сайта: React + TypeScript + Tailwind (быстрая поддержка и лёгкое масштабирование под ваши будущие блоки).
- Все CTA ориентированы на PWA-сценарий (без сторов), как вы просили.
- Телефон и email в футере оставлены как заглушка до подстановки реальных данных перед релизом.

## [2026-04-14] - Палитра как в шаблоне
**What was done:**
- Обновлена цветовая схема лендинга с тёмной на светлую, ближе к шаблону референса.
- Введены мягкие бордеры, светлые карточки и фиолетовый акцент для CTA/ссылок.
- Подправлены вторичные цвета текста для лучшей читаемости и визуального соответствия дизайну.

**Files modified:**
- `src/App.tsx` — заменены основные Tailwind-классы цветов во всех блоках и в футере.
- `src/index.css` — обновлён фоновый цвет страницы под светлую тему.

**Notes:**
- Контент и структура блоков не изменялись, правки затронули только визуальную палитру.

## [2026-04-14] - Фиксированный top-bar и якоря
**What was done:**
- Добавлен фиксированный top-bar в верхней части страницы.
- В top-bar добавлены якорные ссылки на разделы: `О программе` и `Как это работает`.
- Добавлена отдельная кнопка `Войти в приложение` с переходом на `app.slim-spark.ru`.
- Проставлены `id` и `scroll-mt` для корректной прокрутки к секциям с учётом фиксированной шапки.

**Files modified:**
- `src/App.tsx` — добавлены header/nav, якорные ссылки и идентификаторы секций.

**Notes:**
- Сборка и линтер проходят без ошибок после правок.

## [2026-04-14] - Шире блоки и адаптив
**What was done:**
- Расширен основной контейнер лендинга, чтобы блоки выглядели шире на десктопе.
- Улучшена адаптивность фиксированной шапки для телефонов и планшетов.
- Добавлены правки по переносам и выравниванию элементов top-bar на узких экранах.

**Files modified:**
- `src/App.tsx` — увеличена ширина контейнера (`max-w-7xl`) и доработан responsive top-bar.

**Notes:**
- Сетка секций и карточек корректно переходит в одну колонку на мобильных устройствах.

## [2026-04-14] - Оранжевые кнопки и фон
**What was done:**
- Обновлён акцентный цвет всех основных кнопок на оранжевый.
- Обновлён основной задний фон сайта на `#FFF8F0`.

**Files modified:**
- `src/App.tsx` — заменены цвета кнопок CTA и кнопки в top-bar.
- `src/index.css` — установлен фоновый цвет `body: #FFF8F0`.

**Notes:**
- Контент и структура блоков не изменялись, правки затронули только цветовую тему.

## [2026-04-14] - Фон блоков #FFFFF0
**What was done:**
- Обновлён фон всех основных секций и внутренних карточек на `#FFFFF0`.

**Files modified:**
- `src/App.tsx` — изменены цвета фона у секций, карточек преимуществ, шагов и блока вопросов.

**Notes:**
- Фон страницы (`#FFF8F0`) и оранжевые кнопки сохранены без изменений.

## [2026-04-14] - Ширина блоков как в макете
**What was done:**
- Подогнана максимальная ширина контейнера под макет с точным значением `1126px`.
- Синхронизирована ширина top-bar и основного контента для ровного визуального ритма.

**Files modified:**
- `src/App.tsx` — `max-w` контейнера и шапки изменён на `max-w-[1126px]`.

**Notes:**
- Адаптивное поведение на мобильных и планшетах сохранено.

## [2026-04-14] - Дополнительно увеличена ширина блоков
**What was done:**
- Увеличена максимальная ширина контейнера лендинга и шапки до `1240px` для более широкого отображения блоков.

**Files modified:**
- `src/App.tsx` — `max-w` изменён с `1126px` на `1240px`.

**Notes:**
- Адаптив на мобильных/планшетах сохранён, изменения касаются только десктопной ширины.

## [2026-04-14] - Шрифты Raleway и PT Sans
**What was done:**
- Подключены веб-шрифты `Raleway` и `PT Sans` через Google Fonts.
- Заголовки (`h1-h6`) переведены на `Raleway`.
- Основной текст интерфейса переведён на `PT Sans`.

**Files modified:**
- `src/index.css` — добавлен импорт шрифтов и обновлены font-family для body и заголовков.

**Notes:**
- Исправлен порядок CSS `@import`, чтобы сборка проходила без предупреждений.

## [2026-04-14] - Возврат header и последних цветов
**What was done:**
- Возвращён фиксированный top-bar с якорными ссылками (`О программе`, `Как это работает`) и кнопкой `Войти в приложение`.
- Восстановлена светлая цветовая тема: фон страницы `#FFF8F0`, фон блоков `#FFFFF0`, оранжевые акценты на кнопках.
- Возвращена увеличенная ширина контейнера (`1240px`) для шапки и контентных блоков.

**Files modified:**
- `src/App.tsx` — восстановлены header, якоря, ширина контейнера и цветовая схема.
- `src/index.css` — восстановлен светлый фон страницы.

**Notes:**
- Верстка и сборка проверены после возврата конфигурации.

## [2026-04-14] - Увеличен padding-block секций
**What was done:**
- Обновлены основные секции лендинга с вертикальным отступом по аналогии `padding-block: calc(var(--spacing) * 20)`.
- Применён эквивалент Tailwind: `py-20` (с сохранением горизонтальных отступов `px-6/md:px-10`).

**Files modified:**
- `src/App.tsx` — изменены отступы у всех основных `section` блоков.

**Notes:**
- Цвета, ширина контейнера и фиксированный header сохранены без изменений.

## [2026-04-14] - Контейнер сделан шире
**What was done:**
- Дополнительно увеличена максимальная ширина контейнера лендинга для более просторной композиции.

**Files modified:**
- `src/App.tsx` — `max-w` у header/main/footer изменён с `1240px` на `1320px`.

**Notes:**
- Адаптив и текущая цветовая схема сохранены.

## [2026-04-14] - Чередование фонов секций
**What was done:**
- Настроено чередование фоновых цветов основных блоков лендинга по макету.
- Второй и четвертый блоки получили альтернативный оттенок фона для визуального ритма.

**Files modified:**
- `src/App.tsx` — обновлены фоновые классы у секций `О программе` и `Истории успеха`.

**Notes:**
- Остальные настройки цветов, ширины контейнера и header оставлены без изменений.

## [2026-04-14] - Изображение в первый экран
**What was done:**
- Добавлено изображение `main.png` в секцию `Вы устали бороться с собой` с размещением справа от текста.
- Первый экран перестроен в двухколоночный адаптивный layout: текст слева, изображение справа.
- Изображению добавлены скруглённые углы и мягкая тень.

**Files modified:**
- `src/App.tsx` — добавлен блок `img` (`/image/main.png`) и обновлена сетка hero-секции.

**Notes:**
- На мобильных устройствах картинка автоматически переходит под текстовый контент.

## [2026-04-14] - Смягчены цвета заголовков
**What was done:**
- Для крупных заголовков секций добавлен мягкий тёплый градиент вместо плоского тёмного цвета.
- Обновлён стиль заголовка первого экрана `Вы устали бороться с собой` и остальных `h2` секций.

**Files modified:**
- `src/App.tsx` — добавлен `headingGradientClassName` и применён к ключевым заголовкам.

**Notes:**
- Контраст и читаемость сохранены на светлом фоне, акцентные цвета кнопок не менялись.

## [2026-04-14] - Заголовки сделаны светлее
**What was done:**
- Осветлён градиент заголовков для более мягкого визуального акцента.
- Сохранён единый стиль всех ключевых заголовков секций.

**Files modified:**
- `src/App.tsx` — обновлены оттенки `headingGradientClassName`.

**Notes:**
- Читаемость заголовков сохранена, контент и layout не изменялись.

## [2026-04-14] - Анимация загрузки секций
**What was done:**
- Добавлена плавная анимация появления (`fade + slide up`) для основных секций при загрузке страницы.
- Для первого экрана добавлено поэтапное появление контента с задержками для более аккуратного визуального входа.
- Учтён `prefers-reduced-motion`: анимации отключаются для пользователей, которые предпочитают минимальное движение.

**Files modified:**
- `src/index.css` — добавлены keyframes, utility-классы анимации и reduced-motion fallback.
- `src/App.tsx` — применены анимационные классы к hero-элементам и основным секциям.

**Notes:**
- Анимации не влияют на контент, адаптив и текущую цветовую схему.

## [2026-04-14] - Логотип в header
**What was done:**
- В шапке заменён текстовый бренд `Slim-Spark` на изображение `logo.png`.
- Логотип оставлен кликабельным, с переходом к началу страницы.

**Files modified:**
- `src/App.tsx` — обновлена ссылка-бренд в header, добавлен `img` `/image/logo.png`.

**Notes:**
- Размер логотипа ограничен классом `h-10`, чтобы сохранить аккуратную высоту шапки.

## [2026-04-14] - Логотип увеличен в 1.5 раза
**What was done:**
- Увеличен размер логотипа в header примерно в 1.5 раза.

**Files modified:**
- `src/App.tsx` — высота логотипа изменена с `h-10` (40px) на `h-[60px]`.

**Notes:**
- Пропорции логотипа сохранены (`w-auto object-contain`).

## [2026-04-14] - Компактный header и центрирование logo
**What was done:**
- Уменьшена высота header за счёт снижения вертикальных внутренних отступов.
- Логотип выровнен по центру с отступами сверху/снизу внутри бренд-ссылки.
- Размер логотипа подогнан под новый компактный header.

**Files modified:**
- `src/App.tsx` — `py-3` изменён на `py-1`, ссылка-бренд сделана `flex items-center py-1`, логотип изменён на `h-12`.

**Notes:**
- Кликабельность логотипа и поведение навигации сохранены.

## [2026-04-14] - Увеличен логотип в header
**What was done:**
- Логотип в шапке увеличен для более заметного брендинга.
- Немного увеличен вертикальный отступ шапки, чтобы сохранить визуальный баланс.

**Files modified:**
- `src/App.tsx` — размер логотипа изменён на `h-16`, отступ header изменён на `py-2`.

**Notes:**
- Навигация и кликабельность логотипа сохранены.

## [2026-04-14] - Уменьшена высота header
**What was done:**
- Уменьшена высота шапки за счёт уменьшения вертикального внутреннего отступа контейнера.

**Files modified:**
- `src/App.tsx` — `py-2` изменён на `py-1` в контейнере header.

**Notes:**
- Размер логотипа оставлен крупным, изменена только высота header.

## [2026-04-14] - Подключен новый файл логотипа
**What was done:**
- В header подключён обновлённый файл логотипа `logo_.png` вместо предыдущего `logo.png`.

**Files modified:**
- `src/App.tsx` — путь изображения логотипа изменён на `/image/logo_.png`.

**Notes:**
- Текущий размер и поведение логотипа в шапке сохранены.

## [2026-04-14] - Усилена объемность изображения main
**What was done:**
- Улучшено визуальное восприятие изображения `main` в первом экране.
- Добавлена мягкая градиентная подложка, усиленная тень и тонкая рамка.
- Добавлен аккуратный hover-эффект с легким подъёмом и масштабом для ощущения глубины.

**Files modified:**
- `src/App.tsx` — обновлены классы контейнера и изображения в hero-секции.

**Notes:**
- Эффект сделан деликатным, чтобы сохранить чистоту макета и читаемость контента.

## [2026-04-14] - Логотип в footer
**What was done:**
- В footer заменена текстовая надпись `Slim-Spark` на изображение логотипа.

**Files modified:**
- `src/App.tsx` — добавлен `img` `/image/logo_.png` в левом блоке footer.

**Notes:**
- Размер логотипа в footer установлен как `h-12` для визуального баланса с остальным контентом.

## [2026-04-14] - Иконки как в макете
**What was done:**
- Для блока `О программе` добавлены иконки, соответствующие макету: `Brain`, `Utensils`, `Heart`, `Sparkles`.
- Для блока `Как это работает` добавлены иконки по макету: `MessageCircle`, `BookOpen`, `Utensils`, `TrendingUp`.
- Иконки оформлены цветными градиентными плашками по стилистике референса.

**Files modified:**
- `src/App.tsx` — добавлены иконки в структуры карточек и их отрисовка в обоих блоках.
- `package.json` / `package-lock.json` — добавлена зависимость `lucide-react`.

**Notes:**
- Иконки подобраны на основе скомпилированного бандла макета [Landing page design for Slim-Spark](https://cake-ardent-11600489.figma.site/).

## [2026-04-14] - Hover-эффекты и номера шагов как в макете
**What was done:**
- Для карточек с иконками в блоках `О программе` и `Как это работает` добавлены hover-эффекты: легкое увеличение, подъем и усиление тени.
- Для иконок внутри карточек добавлен отдельный hover-эффект увеличения и более выраженная тень.
- В блоке `Как это работает` формат `Шаг 01` заменён на отдельный числовой бейдж (`01`, `02`, `03`, `04`) в верхнем левом углу карточки, как в макете.

**Files modified:**
- `src/App.tsx` — обновлены классы карточек и иконок, добавлены бейджи номеров шагов.

**Notes:**
- Поведение и стилистика настроены в рамках текущей цветовой темы сайта.

## [2026-04-14] - Блоки сделаны немного шире
**What was done:**
- Увеличена максимальная ширина контейнеров сайта для более широкого отображения блоков.

**Files modified:**
- `src/App.tsx` — `max-w` для header, main и footer изменён с `1320px` на `1380px`.

**Notes:**
- Адаптивная логика и текущие отступы сохранены без изменений.

## [2026-04-14] - Градиентный header в стиле сайта
**What was done:**
- Обновлён фон шапки на мягкий фирменный градиент в теплых и свежих оттенках.
- Подправлен цвет нижней границы header для гармонии с новым градиентом.

**Files modified:**
- `src/App.tsx` — обновлены классы фона и бордера у `header`.

**Notes:**
- Читаемость ссылок и кнопки в header сохранена.

## [2026-04-14] - Оформление секции Истории успеха как в макете
**What was done:**
- В секции `Истории успеха` добавлен верхний бейдж `Реальные истории`.
- Заголовок перестроен в двухстрочный формат: первая строка `Истории успеха`, вторая `наших клиентов`.
- Для второй строки применён мягкий градиентный акцент в стиле макета.

**Files modified:**
- `src/App.tsx` — обновлена разметка и стили заголовка в секции `Истории успеха`.

**Notes:**
- Секция сохранена в текущей цветовой теме и общей типографике лендинга.

## [2026-04-14] - Два изображения между секциями
**What was done:**
- Между секциями `О программе` и `Как это работает` добавлен отдельный блок из двух изображений по мотивам макета.
- Подключены файлы `img1.jpg` и `img2.jpg` из `public/image`.
- Для карточек изображений добавлены скругления, мягкая тень и лёгкий hover-zoom.

**Files modified:**
- `src/App.tsx` — добавлена новая секция с двумя изображениями.

**Notes:**
- На мобильных изображения показываются в одну колонку, на планшетах и десктопе — в две.

## [2026-04-14] - Обновление первого блока "Вы устали"
**What was done:**
- В первом блоке удалена ссылка `Открыть приложение →` под описанием.
- Текстовая колонка переразложена по высоте для более равномерного заполнения блока.
- Добавлено вертикальное распределение контента (верхняя текстовая часть + нижняя CTA-зона).

**Files modified:**
- `src/App.tsx` — обновлена структура и layout текстовой части hero-секции.

**Notes:**
- Основной CTA `Хочу попробовать бесплатно →` и описание под ним сохранены.

## [2026-04-14] - Увеличен отступ в первом блоке
**What was done:**
- Увеличен вертикальный отступ между подзаголовком `Пора начать жить.` и следующим абзацем текста в hero-секции.

**Files modified:**
- `src/App.tsx` — отступ изменён с `mt-5` на `mt-8` у абзаца `Вы снова сорвались...`.

**Notes:**
- Изменение точечное, остальные отступы и структура секции не затронуты.

## [2026-04-14] - Увеличен бейдж и поднята CTA-кнопка
**What was done:**
- В первом блоке увеличен текст бейджа `✦ Ем всё что хочу — и худею` примерно на 50%.
- Кнопка `Хочу попробовать бесплатно →` перемещена выше — сразу под основным описанием.
- Подпись про запуск в приложении оставлена ниже кнопки.

**Files modified:**
- `src/App.tsx` — обновлены размер бейджа и вертикальная структура контента в hero-секции.

**Notes:**
- Общий стиль и анимации секции сохранены.

## [2026-04-14] - Кнопка входа в первом блоке
**What was done:**
- Под описанием в первом блоке добавлена кнопка `Войти в приложение`.
- Кнопка оформлена в точности в стиле CTA-кнопки из header.
- Вынесен общий класс `headerCtaClassName` для единообразия кнопок.

**Files modified:**
- `src/App.tsx` — добавлен общий стиль header-кнопки и новая кнопка в hero-секции.

**Notes:**
- Кнопка ведет на `app.slim-spark.ru`, как и остальная навигация приложения.

## [2026-04-14] - Цитата в первом блоке
**What was done:**
- В первом блоке кнопка `Войти в приложение` заменена на оформленную цитату.
- Добавлена цитата:
  - "Безумие — делать одно и то же снова и снова, ожидая другого результата."
  - — Альберт Эйнштейн
- Цитата стилизована в мягком цвете с рамкой и фоном, согласованными с текущей палитрой лендинга.

**Files modified:**
- `src/App.tsx` — кнопка заменена на `blockquote` в hero-секции.

**Notes:**
- Структура блока и остальные CTA-сценарии на странице сохранены.

## [2026-04-14] - Плавная прокрутка по якорям
**What was done:**
- Добавлена плавная прокрутка по якорным ссылкам сайта.
- Учтена доступность: при `prefers-reduced-motion` плавная прокрутка отключается.

**Files modified:**
- `src/index.css` — добавлены правила `html { scroll-behavior: smooth; }` и fallback для reduced motion.

**Notes:**
- Изменение влияет на все якорные ссылки в том числе в header-навигации.

## [2026-04-14] - Пути изображений для GitHub Pages
**What was done:**
- Все ссылки на изображения из `public/image` переведены на путь через `import.meta.env.BASE_URL`.
- Добавлена единая переменная `imageBasePath` для переиспользования путей без ручной правки каждого URL.
- Обновлены пути для логотипа, hero-изображения и двух промежуточных изображений.

**Files modified:**
- `src/App.tsx` — заменены пути `/image/...` на `${import.meta.env.BASE_URL}image/...`.

**Notes:**
- Такой подход корректно работает и локально, и на GitHub Pages с `base`.

## [2026-04-14] - Мобильный header и порядок hero-изображения
**What was done:**
- Переделан header под мобильные устройства: верхняя строка с логотипом и кнопкой, ниже отдельная строка навигации.
- Увеличен верхний отступ контента на мобильных/планшетах, чтобы header не наезжал на первый блок.
- В первом экране изображение `main` переставлено наверх на мобильных и планшетах, а на десктопе оставлено справа.

**Files modified:**
- `src/App.tsx` — обновлены responsive-стили header, `main` отступы и порядок элементов hero-секции.

**Notes:**
- На десктопе текущая компоновка сохраняется, изменения ориентированы на мобильные и планшеты.

## [2026-04-14] - Web-header возвращен к прежнему виду
**What was done:**
- Для web-версии (`md+`) header возвращен к прежней однострочной компоновке: логотип слева, ссылки и кнопка справа.
- Мобильная двухстрочная версия header сохранена как отдельный адаптивный вариант.

**Files modified:**
- `src/App.tsx` — добавлено разделение мобильного и desktop header через responsive-классы.

**Notes:**
- Поведение якорных ссылок и кнопки входа сохранено на всех брейкпоинтах.

## [2026-04-14] - Упрощен мобильный header
**What was done:**
- В мобильной версии header оставлена одна строка.
- Удалены мобильные ссылки `О программе` и `Как это работает`.
- В mobile-header оставлены только логотип и кнопка `Войти в приложение`.

**Files modified:**
- `src/App.tsx` — удален мобильный nav-блок и сохранена однострочная компоновка header.

**Notes:**
- Desktop header (`md+`) не изменялся и остался в прежнем формате.

## [2026-04-14] - Плашка в одну строку на мобилке
**What was done:**
- Плашка `✦ Ем всё что хочу — и худею` в первом блоке адаптирована для мобильной версии в одну строку.
- Уменьшен mobile-шрифт и горизонтальные отступы только для маленьких экранов.
- Добавлен `whitespace-nowrap`, чтобы текст не переносился.

**Files modified:**
- `src/App.tsx` — обновлены responsive-классы плашки в hero-секции.

**Notes:**
- На `md+` сохранён увеличенный размер плашки, как был ранее.
